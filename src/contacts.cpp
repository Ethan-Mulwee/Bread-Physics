#include "bEngine/contacts.hpp"

namespace bEngine {
  ContactResolver::ContactResolver(int iterations) : iterations(iterations) {}

  void ContactResolver::resolveContacts(Contact *contactArray, unsigned numContacts, float time) {
    
  }
} // namespace bEngine
